# Copy this file to `.env` and fill in your key. `.env` is gitignored and must
# never be committed. If GEMINI_API_KEY is left blank/unset, the LLM narrative
# automatically falls back to the deterministic template (no crash).
#
# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
